[project]
name = "fifa21-etl-pipeline"
version = "0.1.0"
description = "FIFA 21 data ETL pipeline"
readme = "README.md"
authors = [
    { name = "xavierkooijman" }
]
requires-python = ">=3.14"
dependencies = [
    "polars>=1.44.1",
    "psycopg[binary]>=3.3.5",
    "python-dotenv>=1.2.3",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
